<%= form_for @animation do |f| %>

<p>
	<%= f.label :title %><br>
	<%= f.text_field :title %>
	<% if @animation.errors.any? %>
	<%= @animation.errors.messages[:title][0] %>
	<% end %>
	<%= f.collection_select(:day, :day, @days, :id, :day) %>
</p>

<p>
	<%= f.submit %>
</p>

<% end %>
